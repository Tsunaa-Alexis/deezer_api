{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<a href="{{ path("addAllTrack", {"artist": artist.id }) }}">Ajouter tout</a>
<h1>Artiste : {{artist.name}}</h1>
<h2>Track enregistrer</h2>
<table class="table">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">id</th>
      <th scope="col">Title</th>
      <th scope="col">Link</th>
      <th scope="col">Duration</th>
    </tr>
  </thead>
  <tbody>
    {% set a = 1 %}
    {% for track_db_n in track_db %}
        <tr>
            <th scope="row">{{a}}</th>
            <th scope="row">{{track_db_n.id}}</th>
            <td>{{track_db_n.title}}</td>
            <td><a href="{{track_db_n.link}}">{{track_db_n.link}}</a></td>
            <td>{{track_db_n.duration}}</td>
            {% set a = a + 1 %}
        </tr>
    {% endfor %}
  </tbody>
</table>

<h1>Nouveautées</h1>
<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">id</th>
            <th scope="col">Title</th>
            <th scope="col">Link</th>
            <th scope="col">Duration</th>
            <th scope="col">#</th>
        </tr>
    </thead>
    <tbody>
      {% set a = 1 %}
      {% for notin_n in notin %}
          <tr>
            <th scope="row">{{a}}</th>
            <td>{{notin_n.id}}</td>
            <td>{{notin_n.title}}</td>
            <td><a href="{{notin_n.link}}">{{notin_n.link}}</a></td>
            <td>{{notin_n.duration}}</td>
            <td><a href="{{ path("addTrack", {"id": notin_n.id,"artist": notin_n.artist.id }) }}">Ajouter</a></td>
            {% set a = a + 1 %}
          </tr>
      {% endfor %}
    </tbody>
</table>
{% endblock %}
